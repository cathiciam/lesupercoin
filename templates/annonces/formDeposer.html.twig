{% extends "base.html.twig" %}
{% block title %}Deposer votre Annonce - Formulaire
{% endblock %}

{% block body %}
	<!-- Section-->
	<section class="py-5">
		<div class="container px-4 px-lg-5 mt-5">
			<h1>Créer votre annonce</h1>

			{{ form_start(formAnnonces) }}
			{{ form_row(formAnnonces.title) }}
			{{ form_row(formAnnonces.content) }}
			{{ form_row(formAnnonces.prix) }}

			<input class="btn btn-success" type="submit" value="Enregistrer">
			{{ form_end(formAnnonces) }}

		</div>
	</section>
{% endblock %}
